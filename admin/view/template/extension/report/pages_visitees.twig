<div class="panel panel-default">
  <div class="panel-heading">
    {# <div class="pull-right">
      <a href="{{ reset }}" class="btn btn-danger btn-xs"><i class="fa fa-refresh"></i> {{ button_reset }}</a>
    </div> #}
    <h3 class="panel-title"><i class="fa fa-filter"></i> {{ heading_title }}</h3>
  </div>
  <div class="panel-body">
    <div class="table-responsive">
      <table class="table table-bordered">
        <thead>
          <tr>
            <td class="text-left">{{ column_titre }}</td>
            <td class="text-left">{{ column_date_visite }}</td>
            <td class="text-left">{{ column_url_page }}</td>
            <td class="text-left">{{ column_id_usager }}</td>
            <td class="text-left">{{ column_adresse_ip }}</td>
          </tr>
        </thead>
        <tbody>
        
        {% if pages %}
        {% for page in pages %}
        <tr>
          <td class="text-left">{{ page.titre }}</td>
          <td class="text-left">{{ page.date_visite }}</td>
          <td class="text-left">{{ page.url_page }}</td>
          <td class="text-left">{{ page.id_usager }}</td>
          <td class="text-left">{{ page.adresse_ip }}</td>
        </tr>
        {% endfor %}
        {% else %}
        <tr>
          <td class="text-center" colspan="4">{{ text_no_results }}</td>
        </tr>
        {% endif %}
        </tbody>
        
      </table>
    </div>
    {# <div class="row">
      <div class="col-sm-6 text-left">{{ pagination }}</div>
      <div class="col-sm-6 text-right">{{ results }}</div>
    </div> #}
  </div>
</div>